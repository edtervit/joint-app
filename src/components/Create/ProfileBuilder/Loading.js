import { Spinner } from "@chakra-ui/react";
import React from "react";

function Loading() {
  return (
    <div>
      <h1>Loading</h1>
      <Spinner></Spinner>
    </div>
  );
}

export default Loading;
